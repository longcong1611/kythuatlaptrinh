#include <bits/stdc++.h>
using namespace std;

int so0(int n)
{
    if (n == 0)
        return 0;
    int N = log(n) / log(5);
}
int main()
{
    int test;
    cin >> test;
    while (test--)
    {
        int n;
        cin >> n;
    }
    return 0;
}